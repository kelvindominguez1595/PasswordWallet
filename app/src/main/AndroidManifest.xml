<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passwordwallet">

    <application
        android:name=".App.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PasswordWallet">
        <activity
            android:name=".Activities.ScrollingActivity2"
            android:exported="false"
            android:label="@string/title_activity_scrolling2"
            android:theme="@style/Theme.PasswordWallet.NoActionBar" />
        <activity
            android:name=".Activities.NewActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.ScrollingActivity"
            android:exported="false"
            android:label="ScrollingActivity"
            android:theme="@style/Theme.PasswordWallet.NoActionBar" />
        <activity
            android:name=".Activities.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".Splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:exported="false" />
    </application>

</manifest>